# Template — do NOT commit real values.
# Populate with: kubectl create secret generic srgan-secrets \
#   --from-literal=JWT_SECRET=<your-jwt-secret> \
#   --from-literal=API_KEY=<your-api-key> \
#   --from-literal=STRIPE_KEY=<your-stripe-secret-key> \
#   --from-literal=S3_ACCESS_KEY=<your-s3-access-key> \
#   --from-literal=S3_SECRET_KEY=<your-s3-secret-key> \
#   -n srgan --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: srgan-secrets
  namespace: srgan
  labels:
    app: srgan-api
type: Opaque
stringData:
  JWT_SECRET: "REPLACE_ME"
  API_KEY: "REPLACE_ME"
  STRIPE_KEY: "REPLACE_ME"
  S3_ACCESS_KEY: "REPLACE_ME"
  S3_SECRET_KEY: "REPLACE_ME"
